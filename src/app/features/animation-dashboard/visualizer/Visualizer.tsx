import s from "./Visualizer.module.css";

const Visualizer = () => {
  return (
    <section className={s.visualizer}>
      <p className={s.p}>Animations Visualizer Here</p>
    </section>
  );
};

export default Visualizer;
